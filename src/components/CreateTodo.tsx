import TodoBar from "./ui/TodoBar"

const CreateTodo = () => {
    return (
        <div className="mt-10 md:w-[50%] md:mx-auto">
            <TodoBar />
        </div>
    )
}

export default CreateTodo